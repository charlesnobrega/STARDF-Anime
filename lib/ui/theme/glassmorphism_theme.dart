import 'package:flutter/material.dart';

/// Glassmorphism color palette for light theme
class GlassmorphismLightColors {
  static const Color primaryGlass = Color(0xFFFFFFFF);
  static const Color secondaryGlass = Color(0xFFF5F5F5);
  static const Color accentGlass = Color(0xFFE8E8E8);
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF666666);
  static const Color borderColor = Color(0xFFE0E0E0);
  static const Color shadowColor = Color(0x1A000000);
}

/// Glassmorphism color palette for dark theme
class GlassmorphismDarkColors {
  static const Color primaryGlass = Color(0xFF1E1E1E);
  static const Color secondaryGlass = Color(0xFF2A2A2A);
  static const Color accentGlass = Color(0xFF3A3A3A);
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB0B0B0);
  static const Color borderColor = Color(0xFF404040);
  static const Color shadowColor = Color(0x33000000);
}

/// Glassmorphism blur values
class GlassmorphismBlur {
  static const double small = 5.0;
  static const double medium = 10.0;
  static const double large = 20.0;
}

/// Glassmorphism opacity values
class GlassmorphismOpacity {
  static const double low = 0.3;
  static const double medium = 0.5;
  static const double high = 0.7;
  static const double veryHigh = 0.85;
}

/// Glassmorphism border radius values
class GlassmorphismRadius {
  static const double small = 8.0;
  static const double medium = 12.0;
  static const double large = 20.0;
  static const double extraLarge = 28.0;
}

/// Glassmorphism spacing values
class GlassmorphismSpacing {
  static const double xs = 4.0;
  static const double sm = 8.0;
  static const double md = 12.0;
  static const double lg = 16.0;
  static const double xl = 24.0;
  static const double xxl = 32.0;
}
